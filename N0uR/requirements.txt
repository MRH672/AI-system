# Core dependencies
numpy>=1.21.0
scikit-learn>=1.0.0
nltk>=3.7
flask>=2.0.0
flask-cors>=3.0.0

# Optional dependencies for enhanced features
pandas>=1.3.0
python-dotenv>=0.19.0

# Note: sqlite3 is included with Python by default
# Note: json is included with Python by default
